
// Find String Length

#include <stdio.h>
#include <string.h>
int main()
{
    printf("***** String Program *****\n");

    char str[20] = {'k', 'u', 'n', 'a', 'l', 'k', 'k', 'h', 'u'};
    int len;
    len = strlen(str);
    printf("The Length of str is %d", len);
}